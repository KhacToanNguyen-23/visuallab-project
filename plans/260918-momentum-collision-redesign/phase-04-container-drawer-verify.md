# Phase 4: Container, Drawer Sync & Verification

## Objectives
- Tạo `frontend/src/components/simulations/momentum-collision/MomentumCollisionLab.tsx` làm container chính.
- Re-export trong `frontend/src/components/simulations/MomentumCollisionLab.tsx`.
- Cập nhật `frontend/src/components/assignment/FloatingAssignmentDrawer.tsx` để hỗ trợ `sim-momentum-collision` và tải `edulab_momentum_grade_result`.
- Đăng ký 3 tính năng trong `feature_list.json`.
- Chạy `npm run build` để kiểm tra compile TypeScript và bundle 0 lỗi.
