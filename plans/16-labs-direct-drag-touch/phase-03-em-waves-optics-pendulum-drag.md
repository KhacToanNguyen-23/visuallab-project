# Phase 3: Electromagnetism, Waves, Optics, Pendulum & Sandbox Direct Drag Controls

## Goal
Implement pointer drag controls into:
1. `InductionLab.tsx` (Drag bar magnet in/out of coil continuously).
2. `PhetEmfLab.tsx` (Drag knife switch lever & potentiometer dial).
3. `SoundResonanceLab.tsx` (Drag water reservoir up/down).
4. `WaveInterferenceLab.tsx` (Drag double caliper jaws over fringe pattern).
5. `PhetRefractionLab.tsx` (Drag laser pointer beam head).
6. `PhetPendulumLab.tsx` (Drag pendulum bob to set initial angle $\theta_0$).
7. `DragDropWorkbenchEngine.tsx` / `UniversalWorkbenchPage.tsx` (Drag components & wire leads).

## Verification
- Run `npm run build` in `frontend/` to confirm 0 compilation errors.
