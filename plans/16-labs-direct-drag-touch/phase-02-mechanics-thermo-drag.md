# Phase 2: Mechanics & Thermodynamics Direct Drag Controls

## Goal
Implement pointer drag controls into:
1. `SpeedWorkbench3D.tsx` (Drag steel ball up incline track, release pointer to roll).
2. `FreeFallCanvas.tsx` (Drag steel ball to electromagnet, click release).
3. `MomentumCollisionLab.tsx` (Drag glider car A launcher spring).
4. `PhetSpringLab.tsx` (Drag mass hanger vertically to stretch spring).
5. `BoyleMariotteLab.tsx` (Drag piston handle up/down in cylinder).
6. `LatentHeatLab.tsx` (Drag ice block into beaker).
7. `SpecificHeatLab.tsx` (Drag heating element & turn dial).

## Verification
- Run `npm run build` in `frontend/` to confirm 0 compilation errors.
